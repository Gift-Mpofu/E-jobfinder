{
  "entities": {
    "Admin": {
      "title": "Admin",
      "description": "A document representing an administrative user. The presence of a document with the user's UID as the ID grants global permissions.",
      "type": "object",
      "properties": {
        "isAdmin": {
          "type": "boolean",
          "description": "Flag indicating administrative status."
        }
      },
      "required": ["isAdmin"]
    },
    "UserCV": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "UserCV",
      "type": "object",
      "description": "Represents a user's uploaded Curriculum Vitae (CV) document within the E-Jobfinder Pro application.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the UserCV entity."
        },
        "userId": {
          "type": "string",
          "description": "Reference to the external user system's identifier for the user who uploaded this CV. (Relationship: User 1:N UserCV)"
        },
        "fileName": {
          "type": "string",
          "description": "The original file name of the uploaded CV (e.g., 'JohnDoe_CV.pdf')."
        },
        "fileStoragePath": {
          "type": "string",
          "description": "The storage path or URL where the CV file is stored (e.g., a cloud storage URL or a path within a storage bucket).",
          "format": "uri"
        },
        "uploadDate": {
          "type": "string",
          "description": "The date and time when the CV was uploaded.",
          "format": "date-time"
        },
        "flagged": {
          "type": "boolean",
          "description": "Whether the CV has been flagged as suspicious by an admin."
        }
      },
      "required": [
        "id",
        "userId",
        "fileName",
        "fileStoragePath",
        "uploadDate"
      ]
    },
    "JobDescription": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "JobDescription",
      "type": "object",
      "description": "Represents a job description text pasted or entered by a user for compatibility analysis.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the JobDescription entity."
        },
        "userId": {
          "type": "string",
          "description": "Reference to the external user system's identifier for the user who provided this job description. (Relationship: User 1:N JobDescription)"
        },
        "title": {
          "type": "string",
          "description": "An optional user-provided or automatically extracted title for the job description (e.g., 'Senior Software Engineer')."
        },
        "descriptionText": {
          "type": "string",
          "description": "The full text content of the job description as provided by the user."
        },
        "pasteDate": {
          "type": "string",
          "description": "The date and time when the job description text was entered or pasted.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "userId",
        "descriptionText",
        "pasteDate"
      ]
    },
    "CVJobMatch": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "CVJobMatch",
      "type": "object",
      "description": "Stores the results of a compatibility analysis performed between a user's CV and a specific job description.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the CVJobMatch entity."
        },
        "userCVId": {
          "type": "string",
          "description": "Reference to the UserCV entity that was used for this analysis. (Relationship: UserCV 1:N CVJobMatch)"
        },
        "jobDescriptionId": {
          "type": "string",
          "description": "Reference to the JobDescription entity that was used for this analysis. (Relationship: JobDescription 1:N CVJobMatch)"
        },
        "matchScore": {
          "type": "number",
          "description": "The compatibility score (e.g., a percentage from 0 to 100) indicating how well the CV matches the job description."
        },
        "strengths": {
          "type": "array",
          "description": "A list of key strengths, skills, or experiences identified in the CV that are highly relevant to the job description.",
          "items": {
            "type": "string"
          }
        },
        "missingKeywords": {
          "type": "array",
          "description": "A list of important keywords, skills, or requirements from the job description that are missing or underrepresented in the CV.",
          "items": {
            "type": "string"
          }
        },
        "improvementSuggestions": {
          "type": "string",
          "description": "Detailed textual suggestions for improving the CV's alignment with the specific job description, including reasons for incorporating certain information."
        },
        "analysisDate": {
          "type": "string",
          "description": "The date and time when this match analysis was performed.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "userCVId",
        "jobDescriptionId",
        "matchScore",
        "strengths",
        "missingKeywords",
        "improvementSuggestions",
        "analysisDate"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "/admins/{adminId}": {
      "schema": "Admin",
      "description": "Collection of administrative users identified by their UID."
    },
    "/users/{userId}/userCVs/{userCVId}": {
      "schema": "UserCV",
      "description": "User-specific CV documents."
    },
    "/users/{userId}/jobDescriptions/{jobDescriptionId}": {
      "schema": "JobDescription",
      "description": "User-specific job descriptions."
    },
    "/users/{userId}/cvJobMatches/{cvJobMatchId}": {
      "schema": "CVJobMatch",
      "description": "Compatibility analysis results."
    }
  }
}